---

publish: true

draft: false

---

```cpp
/**
 * @brief Inserts a key into the skip list.
 * @details Inserts a key while maintaining sorted order.
 * Note: `Insert` will not insert the key if it already exists.
 *
 * @param key key to insert.
 * @return true if successful.
 */
```
- `@brief`：填写简短描述。
- `@details`：完整行为说明。
- `@param`：填写参数。
- `@return`：填写返回类型。
- `Note`：补充描述。